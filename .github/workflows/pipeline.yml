name: Complete CI/CD Pipeline

on:
  push:
    branches: [dev]
  workflow_dispatch:  # Permite ejecución manual

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  security-analysis:
    name: 🔍 Security Analysis on Dev
    runs-on: ubuntu-latest
    
    outputs:
      is_safe: ${{ steps.scan.outputs.is_safe }}
      scanned_count: ${{ steps.scan.outputs.scanned_count }}
      commit_sha: ${{ github.sha }}

    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: 📥 Install Python dependencies
      run: |
        pip install -r requirements.txt 2>/dev/null || true
        pip install joblib datasets scikit-learn==1.5.2 scipy requests pandas 2>/dev/null || true

    - name: 🔍 Security Scan All Files
      id: scan
      run: |
        echo "🔄 Iniciando escaneo de seguridad completo..."
        echo "📂 Rama actual: ${{ github.ref_name }}"
        
        # Contar archivos
        JS_COUNT=$(find . -name "*.js" -not -path "*/node_modules/*" -not -path "*/.next/*" -not -path "*/.venv/*" 2>/dev/null | wc -l || echo "0")
        PY_COUNT=$(find . -name "*.py" -not -path "*/node_modules/*" -not -path "*/.venv/*" -not -path "*/scripts/*" 2>/dev/null | wc -l || echo "0")
        JAVA_COUNT=$(find . -name "*.java" -not -path "*/node_modules/*" -not -path "*/.venv/*" 2>/dev/null | wc -l || echo "0")
        
        TOTAL=$((JS_COUNT + PY_COUNT + JAVA_COUNT))
        echo "📊 Archivos encontrados:"
        echo "   - JavaScript: $JS_COUNT"
        echo "   - Python: $PY_COUNT"
        echo "   - Java: $JAVA_COUNT"
        echo "   - Total: $TOTAL"
        
        # Simular escaneo (ajusta esto con tu script real)
        echo "🔬 Simulando análisis de seguridad..."
        echo "✅ Todos los archivos son seguros"
        
        echo "scanned_count=$TOTAL" >> $GITHUB_OUTPUT
        echo "is_safe=true" >> $GITHUB_OUTPUT
        
        # Si hubiera vulnerabilidades:
        # echo "is_safe=false" >> $GITHUB_OUTPUT
        # exit 1

    - name: ✅ Send Telegram - Security Passed
      if: success()
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        MESSAGE="✅ *Security Analysis PASSED*%0A%0A"
        MESSAGE="${MESSAGE}📂 *Branch:* ${{ github.ref_name }}%0A"
        MESSAGE="${MESSAGE}📊 *Files scanned:* ${{ steps.scan.outputs.scanned_count }}%0A"
        MESSAGE="${MESSAGE}🛡️ *Status:* ALL SECURE%0A%0A"
        MESSAGE="${MESSAGE}🔄 *Next:* Merging to test branch"
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"

  merge-to-test:
    name: 🔀 Merge Dev to Test
    needs: security-analysis
    runs-on: ubuntu-latest
    if: needs.security-analysis.outputs.is_safe == 'true'

    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: ⚙️ Configure Git
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"

    - name: 🔀 Merge dev into test
      run: |
        echo "🔄 Merging dev to test..."
        
        # Obtener información del commit
        COMMIT_SHA="${{ needs.security-analysis.outputs.commit_sha }}"
        COMMIT_MSG=$(git log -1 --pretty=%B)
        
        # Hacer merge
        git checkout test
        git pull origin test
        git merge --no-ff dev -m "🔀 Auto-merge: dev → test [Security Passed]
        
        ✅ Security Analysis:
        - Files scanned: ${{ needs.security-analysis.outputs.scanned_count }}
        - Vulnerabilities: 0
        - Status: SECURE
        
        📝 Original commit: $COMMIT_MSG
        
        🔗 SHA: ${COMMIT_SHA:0:8}"
        
        git push origin test
        
        echo "✅ Merge completed successfully!"
        echo "🔗 Test branch updated"

    - name: 📢 Send Telegram - Merge Success
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        MESSAGE="✅ *Merge to Test COMPLETED*%0A%0A"
        MESSAGE="${MESSAGE}🔄 *From:* dev%0A"
        MESSAGE="${MESSAGE}📂 *To:* test%0A"
        MESSAGE="${MESSAGE}📊 *Files scanned:* ${{ needs.security-analysis.outputs.scanned_count }}%0A"
        MESSAGE="${MESSAGE}🛡️ *Security status:* SECURE%0A%0A"
        MESSAGE="${MESSAGE}🧪 *Next:* Running unit tests on test branch"
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"

  run-backend-tests:
    name: 🧪 Backend Tests (Node.js)
    needs: merge-to-test
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout test branch
      uses: actions/checkout@v3
      with:
        ref: test
        fetch-depth: 0

    - name: 🔍 Find Backend Directory
      id: find-backend
      run: |
        echo "🔍 Searching for backend..."
        
        if [ -d "backend" ] && [ -f "backend/package.json" ]; then
          echo "✅ Backend found at: backend/"
          echo "directory=backend" >> $GITHUB_OUTPUT
        elif [ -f "package.json" ] && ( [ -f "index.js" ] || [ -f "server.js" ] || [ -f "app.js" ] ); then
          echo "✅ Backend found at root"
          echo "directory=." >> $GITHUB_OUTPUT
        else
          echo "⚠️ Backend not found"
          echo "directory=none" >> $GITHUB_OUTPUT
        fi

    - name: 🟢 Setup Node.js
      if: steps.find-backend.outputs.directory != 'none'
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: ${{ steps.find-backend.outputs.directory }}/package.json

    - name: 📥 Install Backend Dependencies
      if: steps.find-backend.outputs.directory != 'none'
      run: |
        echo "📦 Installing backend dependencies..."
        cd ${{ steps.find-backend.outputs.directory }}
        npm ci --silent || npm install --silent
        echo "✅ Dependencies installed"

    - name: 🧪 Run Backend Tests
      if: steps.find-backend.outputs.directory != 'none'
      id: backend-tests
      run: |
        echo "🧪 Running backend tests..."
        cd ${{ steps.find-backend.outputs.directory }}
        
        # Check if test script exists
        if npm run | grep -q " test "; then
          echo "🚀 Running npm test..."
          
          # Run tests with timeout
          timeout 300 npm test 2>&1 | tee test-output.log
          TEST_RESULT=${PIPESTATUS[0]}
          
          if [ $TEST_RESULT -eq 0 ]; then
            echo "✅ BACKEND TESTS PASSED"
            echo "passed=true" >> $GITHUB_OUTPUT
          elif [ $TEST_RESULT -eq 124 ]; then
            echo "⏰ TESTS TIMEOUT (5 minutes)"
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "❌ BACKEND TESTS FAILED"
            echo "📄 Last 10 lines of output:"
            tail -10 test-output.log
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          fi
        else
          echo "⚠️ No test script found in package.json"
          echo "📄 Available scripts:"
          npm run
          echo "✅ Skipping backend tests"
          echo "passed=true" >> $GITHUB_OUTPUT
        fi

    - name: 📊 Backend Test Summary
      if: always() && steps.find-backend.outputs.directory != 'none'
      run: |
        echo "📈 BACKEND TEST SUMMARY"
        echo "══════════════════════"
        echo "Directory: ${{ steps.find-backend.outputs.directory }}"
        echo "Status: ${{ steps.backend-tests.outputs.passed == 'true' && '✅ PASSED' || '❌ FAILED' }}"
        
        if [ -f "${{ steps.find-backend.outputs.directory }}/test-output.log" ]; then
          echo "📄 Test output available in: ${{ steps.find-backend.outputs.directory }}/test-output.log"
        fi

    - name: 📢 Send Telegram - Backend Tests
      if: always()
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        if [ "${{ steps.find-backend.outputs.directory }}" = "none" ]; then
          MESSAGE="ℹ️ *Backend Tests SKIPPED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Reason:* Backend not found%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
        elif [ "${{ steps.backend-tests.outputs.passed }}" = "true" ]; then
          MESSAGE="✅ *Backend Tests PASSED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Location:* ${{ steps.find-backend.outputs.directory }}%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
        else
          MESSAGE="❌ *Backend Tests FAILED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Location:* ${{ steps.find-backend.outputs.directory }}%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
          MESSAGE="${MESSAGE}⚠️ *Check logs for details*%0A"
        fi
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"

  run-frontend-tests:
    name: 🧪 Frontend Tests (React)
    needs: merge-to-test
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout test branch
      uses: actions/checkout@v3
      with:
        ref: test
        fetch-depth: 0

    - name: 🔍 Find Frontend Directory
      id: find-frontend
      run: |
        echo "🔍 Searching for frontend..."
        
        if [ -d "frontend" ] && [ -f "frontend/package.json" ]; then
          echo "✅ Frontend found at: frontend/"
          echo "directory=frontend" >> $GITHUB_OUTPUT
        elif [ -d "client" ] && [ -f "client/package.json" ]; then
          echo "✅ Frontend found at: client/"
          echo "directory=client" >> $GITHUB_OUTPUT
        elif [ -f "package.json" ]; then
          # Check if it's a React app
          if grep -q "react-scripts" package.json || grep -q "next" package.json || grep -q "react" package.json; then
            echo "✅ Frontend found at root"
            echo "directory=." >> $GITHUB_OUTPUT
          else
            echo "⚠️ Frontend not found (no React detected)"
            echo "directory=none" >> $GITHUB_OUTPUT
          fi
        else
          echo "⚠️ Frontend not found"
          echo "directory=none" >> $GITHUB_OUTPUT
        fi

    - name: 🟢 Setup Node.js
      if: steps.find-frontend.outputs.directory != 'none'
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: ${{ steps.find-frontend.outputs.directory }}/package.json

    - name: 📥 Install Frontend Dependencies
      if: steps.find-frontend.outputs.directory != 'none'
      run: |
        echo "📦 Installing frontend dependencies..."
        cd ${{ steps.find-frontend.outputs.directory }}
        npm ci --silent || npm install --silent
        echo "✅ Dependencies installed"

    - name: 🧪 Run Frontend Tests
      if: steps.find-frontend.outputs.directory != 'none'
      id: frontend-tests
      run: |
        echo "🧪 Running frontend tests..."
        cd ${{ steps.find-frontend.outputs.directory }}
        
        # Check if test script exists
        if npm run | grep -q " test "; then
          echo "🚀 Running npm test (CI mode)..."
          
          # Set CI environment
          export CI=true
          
          # Run tests with timeout
          timeout 300 npm test -- --watchAll=false --passWithNoTests 2>&1 | tee test-output.log
          TEST_RESULT=${PIPESTATUS[0]}
          
          if [ $TEST_RESULT -eq 0 ]; then
            echo "✅ FRONTEND TESTS PASSED"
            echo "passed=true" >> $GITHUB_OUTPUT
          elif [ $TEST_RESULT -eq 124 ]; then
            echo "⏰ TESTS TIMEOUT (5 minutes)"
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "❌ FRONTEND TESTS FAILED"
            echo "📄 Last 10 lines of output:"
            tail -10 test-output.log
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          fi
        else
          echo "⚠️ No test script found in package.json"
          echo "📄 Available scripts:"
          npm run
          echo "✅ Skipping frontend tests"
          echo "passed=true" >> $GITHUB_OUTPUT
        fi

    - name: 📊 Frontend Test Summary
      if: always() && steps.find-frontend.outputs.directory != 'none'
      run: |
        echo "📈 FRONTEND TEST SUMMARY"
        echo "═══════════════════════"
        echo "Directory: ${{ steps.find-frontend.outputs.directory }}"
        echo "Status: ${{ steps.frontend-tests.outputs.passed == 'true' && '✅ PASSED' || '❌ FAILED' }}"
        
        if [ -f "${{ steps.find-frontend.outputs.directory }}/test-output.log" ]; then
          echo "📄 Test output available in: ${{ steps.find-frontend.outputs.directory }}/test-output.log"
        fi

    - name: 📢 Send Telegram - Frontend Tests
      if: always()
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        if [ "${{ steps.find-frontend.outputs.directory }}" = "none" ]; then
          MESSAGE="ℹ️ *Frontend Tests SKIPPED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Reason:* Frontend not found%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
        elif [ "${{ steps.frontend-tests.outputs.passed }}" = "true" ]; then
          MESSAGE="✅ *Frontend Tests PASSED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Location:* ${{ steps.find-frontend.outputs.directory }}%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
        else
          MESSAGE="❌ *Frontend Tests FAILED*%0A%0A"
          MESSAGE="${MESSAGE}📂 *Location:* ${{ steps.find-frontend.outputs.directory }}%0A"
          MESSAGE="${MESSAGE}🔗 *Branch:* test%0A"
          MESSAGE="${MESSAGE}⚠️ *Check logs for details*%0A"
        fi
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"

  merge-to-main:
    name: 🚀 Merge Test to Main
    needs: [run-backend-tests, run-frontend-tests]
    runs-on: ubuntu-latest
    if: |
      (needs.run-backend-tests.result == 'success' || needs.run-backend-tests.result == 'skipped') &&
      (needs.run-frontend-tests.result == 'success' || needs.run-frontend-tests.result == 'skipped')

    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: ⚙️ Configure Git
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"

    - name: 🔀 Merge test into main
      run: |
        echo "🚀 Merging test to main..."
        
        # Obtener información actual
        git checkout test
        LATEST_COMMIT=$(git log -1 --pretty=format:"%H")
        LATEST_MSG=$(git log -1 --pretty=format:"%B")
        
        # Crear mensaje de merge
        MERGE_MSG="🚀 Auto-merge: test → main [Pipeline Successful]
        
        📊 PIPELINE RESULTS:
        
        🔍 SECURITY ANALYSIS:
        - Status: ✅ PASSED
        - Files scanned: ${{ needs.security-analysis.outputs.scanned_count }}
        
        🧪 UNIT TESTS:
        - Backend: ${{ needs.run-backend-tests.result == 'success' && '✅ PASSED' || '⚠️ SKIPPED' }}
        - Frontend: ${{ needs.run-frontend-tests.result == 'success' && '✅ PASSED' || '⚠️ SKIPPED' }}
        
        📦 DEPLOYMENT INFO:
        - Source branch: test
        - Target branch: main
        - Pipeline run: #${{ github.run_id }}
        - Timestamp: $(date '+%Y-%m-%d %H:%M:%S')
        
        🔗 Original commit: ${LATEST_COMMIT:0:8}
        ${LATEST_MSG}"
        
        # Realizar merge
        git checkout main
        git pull origin main
        git merge --no-ff test -m "$MERGE_MSG"
        
        # Push a main
        git push origin main
        
        # Crear tag
        TAG_NAME="release-v$(date '+%Y%m%d.%H%M%S')"
        git tag -a "$TAG_NAME" -m "Production release - Pipeline #${{ github.run_id }}"
        git push origin "$TAG_NAME"
        
        echo "✅ Merge completed!"
        echo "🏷️ Tag created: $TAG_NAME"

    - name: 🎉 Send Telegram - Deployment Success
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        TAG_NAME="release-v$(date '+%Y%m%d.%H%M%S')"
        
        MESSAGE="🎉 *DEPLOYMENT SUCCESSFUL!*%0A%0A"
        MESSAGE="${MESSAGE}🚀 *Complete CI/CD Pipeline FINISHED*%0A"
        MESSAGE="${MESSAGE}══════════════════════════════════%0A%0A"
        MESSAGE="${MESSAGE}📊 *SUMMARY:*%0A"
        MESSAGE="${MESSAGE}🔍 *Security Analysis:* ✅ PASSED%0A"
        MESSAGE="${MESSAGE}   ├─ Files scanned: ${{ needs.security-analysis.outputs.scanned_count }}%0A"
        MESSAGE="${MESSAGE}   └─ Vulnerabilities: 0%0A"
        MESSAGE="${MESSAGE}🧪 *Unit Tests:*%0A"
        MESSAGE="${MESSAGE}   ├─ Backend: ${{ needs.run-backend-tests.result == 'success' && '✅ PASSED' || '⚠️ SKIPPED' }}%0A"
        MESSAGE="${MESSAGE}   └─ Frontend: ${{ needs.run-frontend-tests.result == 'success' && '✅ PASSED' || '⚠️ SKIPPED' }}%0A"
        MESSAGE="${MESSAGE}📦 *Deployment:*%0A"
        MESSAGE="${MESSAGE}   ├─ From: test%0A"
        MESSAGE="${MESSAGE}   ├─ To: main ✅%0A"
        MESSAGE="${MESSAGE}   └─ Tag: ${TAG_NAME}%0A%0A"
        MESSAGE="${MESSAGE}🏁 *STATUS:* ✅ PRODUCTION READY%0A"
        MESSAGE="${MESSAGE}🔗 *View commit:* $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/commit/${{ github.sha }}"
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"

  handle-failure:
    name: 🚨 Handle Pipeline Failure
    runs-on: ubuntu-latest
    if: failure()
    
    steps:
    - name: 📢 Send Failure Notification
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        MESSAGE="🚨 *PIPELINE FAILED*%0A%0A"
        MESSAGE="${MESSAGE}❌ *Status:* FAILED%0A"
        MESSAGE="${MESSAGE}📂 *Branch:* ${{ github.ref_name }}%0A"
        MESSAGE="${MESSAGE}🆔 *Run ID:* ${{ github.run_id }}%0A"
        MESSAGE="${MESSAGE}🔧 *Workflow:* ${{ github.workflow }}%0A%0A"
        MESSAGE="${MESSAGE}⚠️ *Pipeline has been stopped*%0A"
        MESSAGE="${MESSAGE}🔗 *Check logs:*%0A"
        MESSAGE="${MESSAGE}$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
        
        curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
          -d "chat_id=${TELEGRAM_CHAT_ID}" \
          -d "text=${MESSAGE}" \
          -d "parse_mode=Markdown"